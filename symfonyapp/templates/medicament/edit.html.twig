{% extends 'base.html.twig' %}

{% block title %}Edit Medicament{% endblock %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <h1 class="font-weight-light">Edit Medicament</h1>
            
            {{ include('medicament/_form.html.twig', {'button_label': 'Update'}) }}
            
            <a class="btn btn-primary" href="{{ path('medicament') }}">Back to list</a>
    
            {{ include('medicament/_delete_form.html.twig') }}
        </div>
    </div>



{% endblock %}
